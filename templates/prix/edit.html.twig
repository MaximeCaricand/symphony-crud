{% extends 'base.html.twig' %}

{% block title %}Modification Prix{% endblock %}

{% block body %}
    <h1 class="mx-auto" style="width: 200px; margin-top: 30px; margin-bottom: 30px;">Prix</h1>

    <div class="border border-primary rounded" style="padding: 50px">
    	{{ include('prix/_form.html.twig', {'button_label': 'Update'}) }}
		<div class="border border-primary" style="margin-top: 25px; position: absolute">
    		{{ include('prix/_delete_form.html.twig') }}
    	</div>
    </div>
{% endblock %}
