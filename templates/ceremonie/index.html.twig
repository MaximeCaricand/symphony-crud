{% extends 'base.html.twig' %}

{% block title %}Ceremonie index{% endblock %}

{% block body %}

    <h1 class="mx-auto" style="width: 200px; margin-top: 30px; margin-bottom: 30px;">Ceremonie</h1>

    <table class="table table-striped table-hover text-center  border-primary">
        <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Nom_ceremonie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ceremonie in ceremonies %}
            <tr>
                <td>{{ ceremonie.id }}</td>
                <td>{{ ceremonie.nomCeremonie }}</td>
                <td>
                    <button type="button" class="btn btn-info btn-sm"><a href="{{ path('ceremonie_show', {'id': ceremonie.id}) }}">Afficher</a></button>
                    <button type="button" class="btn btn-warning btn-sm"><a href="{{ path('ceremonie_edit', {'id': ceremonie.id}) }}">Editer</a></button>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <button type="button" class="btn btn-success btn-block"><a href="{{ path('ceremonie_new') }}">Nouvelle entrée</a></button>
{% endblock %}
