{{ form_start(form) }}

	<p>Nom cérémonie :
	{{ form_widget(form.nom_ceremonie) }}</p>

	<hr/>

	<button class="btn btn-success" style="margin-right: 20px;">{{ button_label|default('Ajouter') }}</button>
   	<button type="button" class="btn btn-danger"><a href="{{ path('ceremonie_index') }}">Annuler</a></button>

{{ form_end(form) }}
