{% extends 'base.html.twig' %}

{% block title %}Ceremonie{% endblock %}

{% block body %}

    <h1 class="mx-auto" style="width: 200px; margin-top: 30px; margin-bottom: 30px;">Ceremonie</h1>

    <table class="table table-striped table-hover border-left border-primary">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ceremonie.id }}</td>
            </tr>
            <tr>
                <th class="th-lg">Nom_ceremonie</th>
                <td>{{ ceremonie.nomCeremonie }}</td>
            </tr>
        </tbody>
    </table>

    <table>
        <tbody>
            <td><button type="button" class="btn btn-success"><a href="{{ path('ceremonie_index') }}">Retour à la liste</a></button></td>
            <td><button type="button" class="btn btn-warning"><a href="{{ path('ceremonie_edit', {'idc': ceremonie.id}) }}">Editer</a></button></td>
            <td>{{ include('ceremonie/_delete_form.html.twig') }}</td>
        </tbody>
    </table>
{% endblock %}
