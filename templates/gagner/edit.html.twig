{% extends 'base.html.twig' %}

{% block title %}Edit Gagner{% endblock %}

{% block body %}
    <h1>Edit Gagner</h1>

    {{ include('gagner/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('gagner_index') }}">back to list</a>

    {{ include('gagner/_delete_form.html.twig') }}
{% endblock %}
